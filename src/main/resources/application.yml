spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot_demon?useTimezone=true&serverTimezone=UTC
    username: root
    password: Senha@1212
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      show_sql: false